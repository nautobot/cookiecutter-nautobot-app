---
extends: "default"
rules:
  comments: "enable"
  empty-values: "disable"
  indentation:
    indent-sequences: "consistent"
  line-length: "disable"
  quoted-strings:
    quote-type: "double"
ignore: |
  .venv/
  {{ cookiecutter.project_slug }}/invoke.mysql.yml
  {{ cookiecutter.project_slug }}/invoke.example.yml
